version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: digigro-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_secret
      MYSQL_DATABASE: digigro_ai
      MYSQL_USER: digigro
      MYSQL_PASSWORD: digigro_secret
    ports:
      - '3306:3306'
    volumes:
      - digigro_mysql_data:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  digigro_mysql_data:
