{
  "name": "digigro-ai",
  "version": "1.0.0",
  "private": true,
  "description": "DIGIGRO AI — Lovable.dev-like AI app builder platform",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm install --prefix backend",
    "build": "npm run db:init --prefix backend",
    "start": "npm run start --prefix backend",
    "db:init": "npm run db:init --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "echo Run backend and frontend in separate terminals: npm run dev:backend && npm run dev:frontend"
  }
}
